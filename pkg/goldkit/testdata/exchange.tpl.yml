# Request comment.
request:
    method: POST
    path: /some/path
    query: key0=val0&key1={{ .val1 }}
    headers:
        - 'Authorization: Bearer token'
    bodyType: json
    body: |
        {
          "key2": "{{ .val2 }}"
        }

# Response comment.
response:
  statusCode: 201
